% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_zerolength.R
\name{is_zerolength}
\alias{is_zerolength}
\title{Check that \code{x} is a zero-length object}
\usage{
is_zerolength(x)
}
\arguments{
\item{x}{object to check.}
}
\value{
\code{TRUE} or \code{FALSE} indicating if \code{x} is a zero-length object.
}
\description{
Check that \code{x} is a zero-length object
}
\section{Notes}{

\link[=length]{Zero-length} objects can have different \link[=typeof]{types}: NULL
(\link{NULL}), logical (\code{logical(0)}), integer (\code{integer(0)}), double
(\code{numeric(0)}), complex (\code{complex(0)}), character (\code{character(0)}), and list
(\code{\link[=list]{list()}} and \code{\link[=data.frame]{data.frame()}}).

\code{""} is \strong{not} a zero-length object: it has a
\code{length} of one despite its \link[=nchar]{width} of zero characters. A data frame
with zero rows is \strong{not} a zero-length object: it has \code{length} equal to the
number of columns. In contrast, a \code{\link{matrix}} with zero rows \strong{is} a
zero-length object, see the \code{Examples}.

\code{\link[=is.null]{is.null()}} should be used to check that an object is \code{NULL} and, more
generally, \verb{isTRUE(all.equal(x, <zero-length object>))} should be used to
check equality to a zero-length object. Checking equality should \strong{not} be
done by using \code{\link[=Comparison]{==}} because that leads to \code{logical(0)} if any of
the sides contains a zero-length object, which gives an error when used as
complete \link[=Control]{conditional statement}.

\code{all(logical(0))} returns \code{TRUE}, see the \code{Note} in \code{\link[=all]{all()}}. This is also the
case for \code{all(numeric(0))} and \code{all(character(0))} that get coerced to type
\code{logical}.

Although zero-length objects are discarded when combined into a vector with
other values, their types \strong{are} taken into account for type coercion, see
the vignette \emph{Type coercion}:
\code{vignette("type_coercion", package = "checkinput")}. For example, numeric
\code{314} will be coerced to character \code{"314"} when it is combined into a vector
with zero-length \code{character(0)}, such that \code{c(314, character(0))} results in
the character string \code{"314"}, \strong{not} in the numeric value \code{314}.
}

\examples{
is_zerolength(x = character(0)) # TRUE
is_zerolength(x = 0) # FALSE
# A matrix with zero rows *is* a zero-length object ...
is_zerolength(x = as.matrix(data.frame(a = 314))[numeric(0), , drop = FALSE])
# ... whereas a dataframe with zero rows is *not* a zero-length object.
is_zerolength(x = data.frame(a = 314)[numeric(0), , drop = FALSE])

# Zero-length objects affect type coercion.
num <- 314
str(num) # num 314
zerochar <- character(0)
str(zerochar) # chr(0)
str(c(num, zerochar)) # chr "314", not num 314

}
\seealso{
Vignette \emph{Type coercion}: \code{vignette("type_coercion", package = "checkinput")}.

Other collections of checks on type and length:
\code{\link[=all_characters]{all_characters()}},
\code{\link[=all_names]{all_names()}},
\code{\link[=is_logical]{is_logical()}},
\code{\link[=is_natural]{is_natural()}},
\code{\link[=is_number]{is_number()}},
\code{\link[=is_path]{is_path()}}
}
\concept{collections of checks on type and length}
