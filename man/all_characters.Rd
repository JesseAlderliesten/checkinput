% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_characters.R
\name{all_characters}
\alias{all_characters}
\alias{is_character}
\title{Check that \code{x} is character}
\usage{
all_characters(
  x,
  allow_empty = FALSE,
  allow_zero_length = FALSE,
  allow_NA = FALSE
)

is_character(
  x,
  allow_empty = FALSE,
  allow_zero_length = FALSE,
  allow_NA = FALSE
)
}
\arguments{
\item{x}{object to test.}

\item{allow_empty}{\code{TRUE} or \code{FALSE}: allow empty strings (\code{""}) in \code{x}?}

\item{allow_zero_length}{\code{TRUE} or \code{FALSE}: allow
\link[=is_zerolength]{zero-length} \code{x} of the correct type?}

\item{allow_NA}{\code{TRUE} or \code{FALSE}: allow \link{NA}s of the correct type in \code{x}?}
}
\value{
\code{TRUE} or \code{FALSE} indicating if \code{x} is a character vector of the
correct length with only allowed values.
}
\description{
Check that \code{x} is a character vector of the correct length with only allowed
values.
}
\details{
\code{is_character()} and \code{all_characters()} return \code{TRUE} for empty strings (\code{""})
if \code{allow_empty} is \code{TRUE}.

\code{is_character()} returns \code{TRUE} for \code{x} with length one and \code{all_characters()}
returns \code{TRUE} for \code{x} with length larger than zero. Both functions return
\code{TRUE} for numeric-type \link[=is_zerolength]{zero-length} \code{x} if
\code{allow_zero_length} is \code{TRUE}.

\code{is_character()} and \code{all_characters()} return \code{TRUE} for \code{NA_character_} if
\code{allow_NA} is \code{TRUE}.
}
\examples{
is_character("a") # TRUE
is_character(c("a", "b")) # FALSE: incorrect length
all_characters(c("a", "b")) # TRUE
is_character(1) # FALSE: incorrect type
is_character(NA_character_) # FALSE: default 'allow_NA' is FALSE
is_character(NA_character_, allow_NA = TRUE) # TRUE
is_character(NA, allow_NA = TRUE) # FALSE: incorrect type

}
\seealso{
The vignettes \emph{Design choices}:
\code{vignette("design_choices", package = "checkinput")} and
\emph{Type coercion}:
\code{vignette("type_coercion", package = "checkinput")}.

Other collections of checks on type and length:
\code{\link[=all_names]{all_names()}},
\code{\link[=is_logical]{is_logical()}},
\code{\link[=is_natural]{is_natural()}},
\code{\link[=is_number]{is_number()}},
\code{\link[=is_path]{is_path()}},
\code{\link[=is_zerolength]{is_zerolength()}}
}
\concept{collections of checks on type and length}
