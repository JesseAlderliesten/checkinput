% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_characters.R, R/is_character.R
\name{all_characters}
\alias{all_characters}
\alias{is_character}
\title{Check if \code{x} is character}
\usage{
all_characters(x, allow_empty = FALSE, allow_zero = FALSE, allow_NA = FALSE)

is_character(x, allow_empty = FALSE, allow_zero = FALSE, allow_NA = FALSE)
}
\arguments{
\item{x}{object to test.}

\item{allow_empty}{\code{TRUE} or \code{FALSE}: allow empty strings (\code{""}) in \code{x}?}

\item{allow_zero}{\code{TRUE} or \code{FALSE}: allow zero-length values in \code{x}?}

\item{allow_NA}{\code{TRUE} or \code{FALSE}: allow NAs of the correct type in \code{x}?}
}
\value{
\code{TRUE} or \code{FALSE} indicating if \code{x} is a character vector of the
correct length with only allowed character values.
}
\description{
Checks if \code{x} is a character vector of the correct length with only allowed
character values.
}
\details{
The correct length of \code{x} for \code{all_character()} is one or larger if
argument \code{allow_zero} is \code{FALSE} and zero or larger if argument \code{allow_zero}
is \code{TRUE}. The correct length of \code{x} for \code{is_character()} is one if argument
\code{allow_zero} is \code{FALSE} and zero or one if argument \code{allow_zero} is \code{TRUE}.
Argument \code{allow_zero} only has an effect if \code{character(0)} is the only value
in \code{x}, because \code{character(0)} is discarded when it is put in a vector
together with other values.
}
\note{
\code{all_characters()} and \code{is_character()} are mainly used for argument
checking and therefore by default return \code{FALSE} for empty strings,
zero-length character strings, and \code{NA_character_}. In contrast,
\code{\link[base:character]{base::is.character()}} returns \code{TRUE} for these inputs, which can be achieved
with \code{all_characters()} and \code{is_character()} by setting arguments
\code{allow_empty}, \code{allow_zero}, or \code{allow_NA} to \code{TRUE}, respectively.
}
\section{Programming note}{
 This implementation differs from the legacy-code:
\code{NA} is not allowed for arguments \code{allow_empty}, \code{allow_zero}, and
\code{allow_NA}, such that the return will never be \code{NA}.
}

\examples{
is_character("a") # TRUE
is_character(c("a", "b")) # FALSE: incorrect length
all_characters(c("a", "b")) # TRUE
is_character(1) # FALSE: incorrect type
is_character(NA_character_) # FALSE: default 'allow_NA' is FALSE
is_character(NA_character_, allow_NA = TRUE) # TRUE
is_character(NA, allow_NA = TRUE) # FALSE: incorrect type

}
\seealso{
The vignette about type conversion:
\code{vignette("Type_Coercion", package = "checkinput")}.

Other collections of checks on type and length: 
\code{\link{all_names}()},
\code{\link{all_nonnegative}()},
\code{\link{is_logical}()},
\code{\link{is_zerolength}()}
}
\concept{collections of checks on type and length}
