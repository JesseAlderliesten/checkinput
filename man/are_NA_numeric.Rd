% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/are_NA_numeric.R
\name{are_NA_numeric}
\alias{are_NA_numeric}
\title{Are elements of vector \code{x} strictly numeric \code{NA}?}
\usage{
are_NA_numeric(x, allow_NaN = FALSE)
}
\arguments{
\item{x}{object to test.}

\item{allow_NaN}{\code{TRUE} or \code{FALSE}: return \code{TRUE} for \code{NaN}s in \code{x}?}
}
\value{
A boolean vector indicating for each element of \code{x} if it is numeric
\code{NA}, or, if \code{allow_NaN} is \code{TRUE}, is numeric \code{NA} or \code{NaN}. \code{FALSE} for
zero-length \code{x}, and a vector of the same the length of \code{x} filled with
\code{FALSE} for non-numeric \code{x}.
}
\description{
Are elements of vector \code{x} strictly numeric \code{NA}?
}
\details{
Zero-length \code{x} gives \code{FALSE} for \code{are_NA_numeric(x)}. Zero-length \code{x} would
give \code{logical(0)} for \code{is.na(x)} and \code{is.nan(x)}, making those functions less
suitable for use in logical statements.
}
\section{Programming note}{

\code{is.na(x)} is \code{TRUE} for all types of \code{NA} but also for \code{NaN}, whereas
\code{is.nan(x)} is  \code{TRUE} for \code{NaN} but not for any type of \code{NA}.
}

\examples{
are_NA_numeric(c(-Inf, -3, 0, 3, Inf), allow_NaN = TRUE) # all FALSE

are_NA_numeric(c(NA_real_, NA_integer_, NaN)) # c(TRUE, TRUE, FALSE)
are_NA_numeric(c(NA_real_, NA_integer_, NaN), allow_NaN = TRUE) # all TRUE
are_NA_numeric(NA, allow_NaN = TRUE) # FALSE: wrong type

for(x in list(numeric(0), logical(0), character(0))) {
  print(are_NA_numeric(x = x)) # FALSE
}

}
\seealso{
The vignette about type conversion:
\code{vignette("Type_Coercion", package = "checkinput")}. \code{\link[=is.na]{is.na()}} and \code{\link[=is.nan]{is.nan()}}
that are used in this function.

Other collections of checks on type and length: 
\code{\link{all_characters}()},
\code{\link{all_names}()},
\code{\link{all_nonnegative}()},
\code{\link{is_logical}()},
\code{\link{is_zerolength}()}
}
\concept{collections of checks on type and length}
